\* TLC Model Checker Configuration for MCP Lifecycle Specification

\* Constants - Define the model scope
CONSTANTS
  Clients = {c1, c2}
  Servers = {s1}
  MaxRequests = 6

\* Invariants to check
INVARIANTS
  TypeOK
  NoToolsBeforeOperating
  UniqueRequestIDs
  ResponseMatchesRequest
  NoUndeclaredCapabilities
  ValidStateTransitions
  NoBackwardTransition
  CapabilitiesConsistent
  OutstandingRequestsBounded

\* Properties to check (temporal formulas)
PROPERTIES
  EventuallyProgresses
  InitializeEventuallyCompletes

\* Specification to check
SPECIFICATION Spec

\* State constraint to limit state space exploration
\* CONSTRAINT StateConstraint
