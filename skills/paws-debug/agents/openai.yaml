interface:
  display_name: "Paws Debug"
  short_description: "Diagnose a named Paws failure"
  default_prompt: "Use $paws-debug to reproduce and diagnose this failure in Paws; repair it only if requested."
